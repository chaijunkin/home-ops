---
version: "3"

tasks:
  set:
    cmds:
      - cat .config.env | fly secrets import -a {{.FLY_APP}}
    desc: Set secrets for a Fly app [APP=required]
    requires:
      vars:
        - APP
    dir: infrastructure/flyio/{{.APP}}
