# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ocirepository.yaml
  - ./helmrelease.yaml
  - ./externalsecret.yaml
  # Cluster-local llama.cpp services (llmkube CRs) — primary embed/rerank path.
  # Secondary routes (`lmstudio-*`) live in the litellm configmap.
  - ./models/memini-rerank.yaml
  - ./models/memini-embed.yaml
