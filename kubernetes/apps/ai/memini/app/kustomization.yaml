# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ocirepository.yaml
  - ./helmrelease.yaml
  - ./externalsecret.yaml
  # Cluster-local llama.cpp services (llmkube CRs). Embedding stays on LM
  # Studio via LiteLLM; memini-embed.yaml is a documented fallback.
  - ./models/memini-rerank.yaml
