# yaml-language-server: $schema=https://crd.cloudjur.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: profilarr
  namespace: downloads
spec:
  targetNamespace: downloads
  commonMetadata:
    labels:
      app.kubernetes.io/name: profilarr
  path: ./kubernetes/apps/downloads/profilarr/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: false
  interval: 24h
  retryInterval: 1m

  postBuild:
    substitute:
      APP: profilarr
      VOLSYNC_CAPACITY: 2Gi
      KOPIUR_PUID: "1000"
      KOPIUR_PGID: "1000"
  components:
    - ../../../../components/kopiur/backup
    - ../../../../components/kopiur/remote
    # - ../../../../components/volsync
    # - ../../../../components/kopiur/adhoc-snapshot
    - ../../../../components/zeroscaler
  dependsOn:
    - name: openebs
      namespace: storage
