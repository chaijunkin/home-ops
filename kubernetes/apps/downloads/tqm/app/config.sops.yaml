apiVersion: v1
kind: Secret
metadata:
    name: tqm-secret
stringData:
    config.yaml: |-
        notifications:
          detailed: true
          skip_empty_run: true
          service:
            discord:
              webhook_url: http://webhook.default.svc.cluster.local/hooks/tqm-pushover
