# yaml-language-server: $schema=https://crd.cloudjur.com/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: alloy-logs
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Observability Service
    gethomepage.dev/name: Alloy Logs
    gethomepage.dev/description: Logs Collector
    gethomepage.dev/icon: https://raw.githubusercontent.com/homarr-labs/dashboard-icons/main/png/grafana.png
spec:
  hostnames: [alloy-logs.cloudjur.com]
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  rules:
    - backendRefs:
        - name: k8s-monitoring-alloy-logs
          namespace: observability
          port: 12345
---
# yaml-language-server: $schema=https://crd.cloudjur.com/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: alloy-singleton
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Observability Service
    gethomepage.dev/name: Alloy Singleton
    gethomepage.dev/description: Singleton Collector
    gethomepage.dev/icon: https://raw.githubusercontent.com/homarr-labs/dashboard-icons/main/png/grafana.png
spec:
  hostnames: [alloy-singleton.cloudjur.com]
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  rules:
    - backendRefs:
        - name: k8s-monitoring-alloy-singleton
          namespace: observability
          port: 12345
---
# yaml-language-server: $schema=https://crd.cloudjur.com/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: alloy-receiver
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Observability Service
    gethomepage.dev/name: Alloy Receiver
    gethomepage.dev/description: OTLP Receiver
    gethomepage.dev/icon: https://raw.githubusercontent.com/homarr-labs/dashboard-icons/main/png/grafana.png
spec:
  hostnames: [alloy-receiver.cloudjur.com]
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  rules:
    - backendRefs:
        - name: k8s-monitoring-alloy-receiver
          namespace: observability
          port: 12345
---
# yaml-language-server: $schema=https://crd.cloudjur.com/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: alloy-profiles
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Observability Service
    gethomepage.dev/name: Alloy Profiles
    gethomepage.dev/description: Profiles Collector
    gethomepage.dev/icon: https://raw.githubusercontent.com/homarr-labs/dashboard-icons/main/png/grafana.png
spec:
  hostnames: [alloy-profiles.cloudjur.com]
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  rules:
    - backendRefs:
        - name: k8s-monitoring-alloy-profiles
          namespace: observability
          port: 12345
# ---
# # ---------------------------------------------------------------------------
# # Machine ingest — external hosts (e.g. the Strix Halo mini PC, outside the
# # cluster) push telemetry INTO the in-cluster stack via the LAN-only internal
# # gateway. No extAuth: these are agent->backend ingest endpoints reachable only
# # from the trusted internal network (envoy-internal @ 10.10.30.25).
# # ---------------------------------------------------------------------------
# # Loki push: Alloy on the mini PC -> https://loki-ingest.cloudjur.com/loki/api/v1/push
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: loki-ingest
# spec:
#   hostnames: [loki-ingest.cloudjur.com]
#   parentRefs:
#     - name: envoy-internal
#       namespace: network
#       sectionName: https
#   rules:
#     - backendRefs:
#         - name: loki-headless
#           namespace: observability
#           port: 3100
# ---
# # Tempo OTLP/HTTP ingest: Beyla on the mini PC -> https://tempo-ingest.cloudjur.com/v1/traces
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: tempo-ingest
# spec:
#   hostnames: [tempo-ingest.cloudjur.com]
#   parentRefs:
#     - name: envoy-internal
#       namespace: network
#       sectionName: https
#   rules:
#     - backendRefs:
#         - name: tempo
#           namespace: observability
#           port: 4318
# ---
# # Pyroscope ingest: pyroscope-agent (eBPF) on the mini PC -> https://pyroscope-ingest.cloudjur.com/ingest
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: pyroscope-ingest
# spec:
#   hostnames: [pyroscope-ingest.cloudjur.com]
#   parentRefs:
#     - name: envoy-internal
#       namespace: network
#       sectionName: https
#   rules:
#     - backendRefs:
#         - name: pyroscope
#           namespace: observability
#           port: 4040
