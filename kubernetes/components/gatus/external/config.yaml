endpoints:
  - name: "${APP}"
    group: external
    url: "https://${GATUS_SUBDOMAIN:=${APP}}.cloudjur.com${GATUS_PATH:=/}"
    interval: 1m
    client:

    conditions:
      - "[STATUS] == ${GATUS_STATUS:=200}"
