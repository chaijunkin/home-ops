# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ../../kopiur/backup/snapshotpolicy.yaml
  - ../../kopiur/backup/snapshotschedule.yaml
  - ../../kopiur/backup/restore.yaml
  - ../../kopiur/backup/pvc.yaml
patches:
  - target:
      group: helm.toolkit.fluxcd.io
      version: v2
      kind: HelmRelease
    patch: |-
      - op: add
        path: /spec/dependsOn/-
        value:
          name: kopiur-repository
          namespace: storage
