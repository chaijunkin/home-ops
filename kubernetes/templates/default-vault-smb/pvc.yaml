apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: default-vault-smb-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: smb
  volumeName: default-vault-smb-pv-${NAMESPACE}
